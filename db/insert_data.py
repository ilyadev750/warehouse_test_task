# from sqlalchemy.orm import Session
# from models import (Category, MainShelve, Good,
#                     MinorShelve, Order, OrderedGoods)
# from engine import engine

"""Модуль для добавления информации в базу данных"""
# with Session(engine) as session:
#     shelve_1 = MainShelve(name='Т', category_id=7)
#     shelve_2 = MainShelve(name='Б', category_id=8)

#     session.add_all([shelve_1, shelve_2])
#     session.commit()